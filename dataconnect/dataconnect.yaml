specVersion: "v1"
serviceId: "my-site"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "my-site-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
